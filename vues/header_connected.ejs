<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" type="text/css" href="/header_style.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Open+Sans&family=Roboto&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,300,0,0" />
    </head>
    
    <body>
        <header class="main">
            <nav class="links">
                <h2>
                

                <% if(typeof robot !="undefined") { %>
                <a class="logout" href="/game/<%= userConnected.id_users %>/<%= robot.id_robots %>"><span class="material-symbols-outlined">
                    person
                    </span>Robot: <%= robot.nickname%></a>
                <% }%>

                <a class="change-robot" href="/<%= userConnected.id_users %>/select_robot"><span class="material-symbols-outlined">
                    group
                    </span>Change robots</a>
                <% if(typeof robot !="undefined") { %>
                    <a class="logout" href="/game/<%= userConnected.id_users %>/<%= robot.id_robots %>/arena"><span class="material-symbols-outlined">
                        construction
                        </span>Arena !</a>
                <% }%>

                <% if(typeof robot !="undefined") { %>
                    <a class="logout" href="/game/<%= userConnected.id_users %>/<%= robot.id_robots %>/shops"><span class="material-symbols-outlined">
                        storefront
                        </span>Shop</a>
                <% }%>

                <a href="/logout" class="logout"><span class="material-symbols-outlined">
                    logout
                    </span>Logout</a>
            </h2>
            </nav>
        </header>
    </body>

</html>