<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select page</title>
    <link rel="stylesheet" href="/style.css">
</head>
<%- include("header_connected") %>
<body>
<div class="body">
    <div class="create-robot skill-ligne">
    <div><h4><a href="/<%= userConnected.id_users %>/create_robot">Create a robot</a></h4></div>
    <% for( let index=0; index < robots.length; index++ ) { %>
        <div class="skills-ligne selectrobot"><a href="/game/<%= robots[index].id_users %>/<%= robots[index].id_robots %>">
                <div><img src="<%= robots[index].img %>" class="imgrobot"></div>
                <div><%= robots[index].nickname %></div>
                <div>Nickname : <%= robots[index].nickname %></div>
                <div>Level : <%= robots[index].level %></div>
                <div>Experience : <%= robots[index].experience %></div>
                <div>Strength : <%= robots[index].strength %></div>
                <div>Armor : <%= robots[index].armor %></div>
                <div>Agility : <%= robots[index].agility %></div>
        </div>
        <% } %>
    </div>
</div>
</body>

</html>